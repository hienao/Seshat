# 开发环境 Docker Compose 配置
# 使用方法: docker-compose up -d

services:
  # 应用服务（前后端统一容器）
  app:
    build:
      context: .
      dockerfile: deploy/Dockerfile
    ports:
      - "80:80"
    environment:
      # 默认使用 SQLite，可设置为 postgres 切换到 PostgreSQL
      DB_DRIVER: ${DB_DRIVER:-sqlite}
      SQLITE_PATH: ${SQLITE_PATH:-/data/db/basegoapp.db}
      DATA_DIR: ${DATA_DIR:-/data}
      CACHE_DIR: ${CACHE_DIR:-/cache}
      LOG_DIR: ${LOG_DIR:-/cache/logs/app}
      # PostgreSQL 模式下必填
      DATABASE_URL: ${DATABASE_URL:-}
      # 必填：JWT 签名密钥
      JWT_SECRET: ${JWT_SECRET:-dev-secret-key-change-in-production}
      # 可选：Gin 运行模式
      GIN_MODE: ${GIN_MODE:-debug}
      # 可选：后端 API 端口
      SERVER_PORT: ${SERVER_PORT:-8080}
    volumes:
      - app_data:/data
      - app_cache:/cache
    restart: unless-stopped

  # PostgreSQL 数据库（可选：docker compose --profile postgres up -d）
  db:
    profiles: ["postgres"]
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-basegoapp}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  app_data:
  app_cache:
  postgres_data:
